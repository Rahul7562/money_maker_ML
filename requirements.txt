pandas>=2.2.0,<3.0.0
python-binance>=1.0.36
python-dotenv>=1.0.0
numpy>=1.26.0,<2.0.0
aiohttp>=3.9.0
requests>=2.31.0
scikit-learn>=1.4.0
# PyTorch CPU-only for Linux VMs (install separately with: pip install torch --index-url https://download.pytorch.org/whl/cpu)
# Uncomment line below if installing on Windows or with GPU:
# torch>=2.2.0
